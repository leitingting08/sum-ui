import { App } from 'vue'
import {{hump name}} from './Index.vue'
import 'element-plus/theme-chalk/src/base.scss'

{{hump name}}.install = (app: App): void => {
  app.component({{hump name}}.name, {{hump name}})
}

export default {{hump name}}